
{
    "name": "ello/eter",
    "description": "mem/oh",
    "type": "object",
    "additionalProperties": false,
    "required": [ "name", "description" ],
    "properties": {
    "license": {
            "type": ["string", "array"],
            "description": "my licence "
        }
    },
    "scripts": {
        "test": [
            "@php script.php",
            "phpunit"
        ]
    }
}
